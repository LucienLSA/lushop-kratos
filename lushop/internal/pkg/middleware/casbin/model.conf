[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act

# 角色继承（可选）
[role_definition]
g = _, _

[policy_effect]
e = some(where (p.eft == allow))

# RBAC：sub(用户/角色)访问obj(资源路径)执行act(方法)
[matchers]
m = g(r.sub, p.sub) && keyMatch2(r.obj, p.obj) && regexMatch(r.act, p.act)