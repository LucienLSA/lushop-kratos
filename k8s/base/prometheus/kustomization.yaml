apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: prometheus-config
    files:
      - prometheus.yaml
generatorOptions:
  disableNameSuffixHash: true
resources:
  - service.yaml
  - statefulset.yaml

