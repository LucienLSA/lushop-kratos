apiVersion: v1
kind: Service
metadata:
  name: order-service
spec:
  ports:
    - name: http
      port: 8013
      targetPort: 8013
    - name: grpc
      port: 50053
      targetPort: 50053
  selector:
    app.kubernetes.io/name: order-service

