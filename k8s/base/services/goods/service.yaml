apiVersion: v1
kind: Service
metadata:
  name: goods-service
spec:
  ports:
    - name: http
      port: 8012
      targetPort: 8012
    - name: grpc
      port: 50052
      targetPort: 50052
  selector:
    app.kubernetes.io/name: goods-service

