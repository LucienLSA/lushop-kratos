apiVersion: apps/v1
kind: Deployment
metadata:
  name: goods-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: goods-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: goods-service
    spec:
      containers:
        - name: goods
          image: lushop/goods:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8012
              name: http
            - containerPort: 50052
              name: grpc
          volumeMounts:
            - name: config
              mountPath: /data/conf
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            tcpSocket:
              port: 50052
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 50052
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: config
          configMap:
            name: goods-config

