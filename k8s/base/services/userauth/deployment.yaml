apiVersion: apps/v1
kind: Deployment
metadata:
  name: userauth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: userauth-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: userauth-service
    spec:
      containers:
        - name: userauth
          image: lushop/userauth:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8016
              name: http
            - containerPort: 50056
              name: grpc
          env:
            - name: TZ
              value: "Asia/Shanghai"
            - name: LOG_LEVEL
              value: "info"
          volumeMounts:
            - name: config
              mountPath: /data/conf
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          startupProbe:
            tcpSocket:
              port: 50056
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
          readinessProbe:
            tcpSocket:
              port: 50056
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 50056
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
      volumes:
        - name: config
          configMap:
            name: userauth-config

