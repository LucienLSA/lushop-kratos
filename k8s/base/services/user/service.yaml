apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  ports:
    - name: http
      port: 8011
      targetPort: 8011
    - name: grpc
      port: 50051
      targetPort: 50051
  selector:
    app.kubernetes.io/name: user-service

